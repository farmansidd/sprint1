function generateFibonacci(n) {
  // Handle edge cases for 0 or negative inputs
  if (n <= 0) return [];
  if (n === 1) return [0];

  // Initialize the first two terms of the sequence
  const sequence = [0, 1];

  // Start the loop from index 2 up to n
  for (let i =

  return sequence;
}